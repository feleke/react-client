import React from "react";
import { Outlet } from "react-router-dom";

const HomeLayout = () => {
  return <></>;
};

export default HomeLayout;
// what ever placed inside homelayout displayed by Outlet
// whatever set in outlet element it will be displayed on child elements
